import React from 'react'

// Simplified IntelliSense components to prevent errors

export const IntelliSenseProvider = ({ editor, language, children }) => {
  // Basic provider that doesn't break the app
  return null
}

export const SymbolSearch = ({ editor, language }) => {
  return null
}

export const GoToDefinition = ({ editor, language }) => {
  return null
}

export const ErrorSquiggles = ({ editor, language, onErrorsChange }) => {
  return null
}

export const HoverInformation = ({ editor, language }) => {
  return null
}

export default IntelliSenseProvider
